schema {
# The schema’s entry-point for queries. This acts as the public, top-level API
# from which all queries must start.
query: QueryRoot
# The schema’s entry-point for mutations. This acts as the public, top-level API
# from which all mutation queries must start.
mutation: Mutation
}
